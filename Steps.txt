Steps to create ReactContext instances

1. create: AuthContext() function that has authReducer and AuthContextProvider functions
2. create useAuthContext function inside hooks folder that consume the AuthContext


==================== USER SEES ONLY HIS WORK =================================================================  
  Step 1: workoutModel.js
    add new field in the workoutModel.js call it - 	user_id: {
			type: String,
			required: true,
		},

    Step 2: workoutController.js
    - add user_id to the " Workout.create({ title, load, reps });" function
    - add this: const user_id = req.user._id; ot the await Workout.create({ title, load, reps, user_id }); and also add it to the create() function
    - 



